<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hc.upms.DAO.SysUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hc.upms.entity.po.SysUser">

    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        update_user_id,
        update_time,
        create_user_id,
        create_time,
        name, card_id, birthday, phone, sex, login_account, login_pass, is_valid, account_non_expired, credentials_non_expired, account_non_locked, email, remark
    </sql>

</mapper>
